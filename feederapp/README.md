# feederapp

This folder contains the placeholder frontend for the PetFeeder app.

## Next steps

- Replace `index.html` with your actual frontend application.
- If you use a frontend framework, update `render.yaml` to set the proper `buildCommand` and `publishPath`.
- Deploy the app on Render by connecting this repository and creating:
  - a Python `web_service` for the backend
  - a `static_site` for the frontend
